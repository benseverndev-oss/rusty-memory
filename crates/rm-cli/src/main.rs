//! `rmem`'s entry point.
//!
//! Left as a stub: this task builds `rmem.toml` and the config layer that
//! reads it, not the command line itself. A later task fills this in with the
//! subcommands that call [`rm_cli::config`].

fn main() {}
