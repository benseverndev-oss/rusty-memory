/target

# The bake-off is excluded from the workspace, so it builds into its own target
# dir; unanchored so any nested crate's output is covered too.
target/

# Vendored by the copy step in benches/ann-bakeoff/README.md. Third-party
# source, and not ours to commit.
benches/ann-bakeoff/src/goldenhnsw.rs

# The hook stores and their queues. A store is machine-local state, not
# something to share; the settings that wire the hook are personal too.
.rmem/
.claude/settings.local.json

# Agent scaffolding, checked out per-machine and sometimes as embedded
# worktrees -- which `git add -A` will commit as gitlinks that resolve
# nowhere. Ignored rather than remembered, because remembering is what
# failed.
.claude/
.superpowers/
